server:
  error:
    include-stacktrace: on_param
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/economy_on?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG